services:
  agentic-api:
    build: .
    container_name: agentic-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - agentic-api-data:/app/data
    restart: unless-stopped

volumes:
  agentic-api-data:
